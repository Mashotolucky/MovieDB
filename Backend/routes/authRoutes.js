const express = require('express');
const router = express.Router();


/**
 * authenticate user login
 */
router.post('/login', () => {})

/**
 * register new user
 */
router.delete('/signup', () => {})
