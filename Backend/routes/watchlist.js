const express = require('express');
const router = express.Router();

/**
 * Get all watchlist Items by user id
 */
router.get('/:userId', () => {})

/**
 * create a new watchlist item by userId
 */
router.post('/userId', () => {})

/**
 * delete watchlist item by id
 */
router.delete('/:id', () => {})