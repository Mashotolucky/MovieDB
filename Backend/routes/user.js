const express = require('express');
const router = express.Router();

/**
 * Get user data id
 */
router.get('/:id', () => {})

/**
 * delete user by id
 */
router.delete('/:id', () => {})

/**
 * update a user by id
 */
router.put('/:id', () => {})